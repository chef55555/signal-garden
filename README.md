# Signal Garden

A quiet focus timer that turns completed sessions into a small procedural garden. Session progress is stored locally in the browser.

## Development

```bash
npm install
npm run dev
```

The site is built and published to GitHub Pages by the workflow in `.github/workflows/deploy.yml`.
